# Ordered category index. Each key maps to _data/<key>.yml and a page at /<key>/.
# Add a category: create _data/<key>.yml, add an entry here, add <key>.html.
- key: typography
  title: Typography
  blurb: Foundries, pairing tools, and specimens.
