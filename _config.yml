title: Design Resources
description: A personal directory of design resources.
baseurl: /design-resources
url: https://dymartin.github.io

# No theme, no plugins, no CI. Jekyll renders _layouts/default over _data/*.yml,
# GitHub Pages builds it natively. The only dependency is a vendored Pico file.

exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
